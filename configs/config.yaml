project:
  name: kdrama-compass
  version: "1.0.0"

data:
  raw_dir: data/raw
  processed_dir: data/processed
  external_dir: data/external
  files:
    dramas: korean_drama.csv
    reviews: reviews.csv
    actors: wiki_actors.csv
  processed_files:
    dramas: cleaned_dramas.csv
    reviews: cleaned_reviews.csv
    actors: cleaned_actors.csv
    merged: merged_actors_reviews.csv

model:
  artifacts_dir: models/artifacts
  registry_dir: models/registry

api:
  host: "0.0.0.0"
  port: 8000

logging:
  level: INFO
  dir: logs
